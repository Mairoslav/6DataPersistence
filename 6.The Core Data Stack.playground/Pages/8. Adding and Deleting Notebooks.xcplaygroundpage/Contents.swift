//: [Previous](@previous)

import Foundation

// MARK: 8. Adding and Deleting Notebooks
// 00:00 We need to make it so that user can add and delete notebooks. Just a moment ago, we fetched data from the persistent store into a context. What we are going to do now is the reverse, make changes in the context and then ask the context to save the changes to the persistent store. Let's start with adding.
// 00:23 When user taps the addNotebook button, it triggers the addTapped function, where we present an alert that asks the user for a notebook name, ... move to Mooskine 'NotebooksListViewController.swift" below '@IBAction func addTapped' ...

//: [Next](@next)
